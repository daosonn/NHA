# Local development defaults — safe to use as-is on your machine.
# Copy to .env (done automatically by `pnpm bootstrap`).
POSTGRES_USER=nha
POSTGRES_PASSWORD=nha_password
POSTGRES_DB=nha
POSTGRES_PORT=5432
