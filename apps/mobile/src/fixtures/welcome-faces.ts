/**
 * The faces on the way in — Welcome, and the brand pane beside a wide-window
 * sign-in form.
 *
 * A family that already exists, said with people rather than with an
 * illustration: proof, not decoration. They carried no names before, which
 * dropped `Avatar` to its last tier — the striped placeholder — so the row
 * read as four grey hatched circles, which proves nothing.
 *
 * Names, not photographs, and that is a deliberate stop rather than a
 * half-finished job. `Avatar` draws initials on a tint it derives from the
 * name itself, so each face is distinct and warm at 34px, and the app ships
 * no stranger's private photographs to make the point. Real pictures would go
 * in the same shape: give an item a `mediaId` and the photograph wins, since
 * `Avatar` prefers it over the letters.
 *
 * Plain strings, deliberately NOT catalogue keys: `architecture.md` files a
 * name under *the data*, alongside a memo's content and an occasion's title —
 * translating a family's own names is not localisation.
 *
 * Mostly one surname, so the row reads as a single family, with one married
 * in — which is both true of families and the case `Avatar`'s initials were
 * written for: Japanese and Vietnamese names, in either word order. The tint
 * is keyed off the whole name, so three Yamadas still get three colours.
 */
export const welcomeFaces = [
  { id: 'a', name: '山田 花子' },
  { id: 'b', name: '山田 太郎' },
  { id: 'c', name: 'Mai Nguyễn' },
  { id: 'd', name: '山田 健一' },
];
