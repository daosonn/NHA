# Node
node_modules/

# Next.js
.next/
out/

# NestJS / package builds
dist/

# Expo / React Native
.expo/
.expo-shared/
apps/mobile/android/
apps/mobile/ios/
*.keystore
*.jks

# Mockups (large, self-contained HTML 窶・see docs/01-frontend/mockups/README.md)
docs/01-frontend/mockups/*.html

# Python
venv/
.venv/
__pycache__/
*.py[cod]

# Database dumps — real family data, never commit (pnpm db:backup)
backups/

# Environment
.env
.env.local

# Media uploads (local-disk storage for the MVP demo)
apps/api/uploads/

# Seed photos are supplied per machine, never committed — only the README is
# (apps/api/prisma/seed-images/README.md explains why)
apps/api/prisma/seed-images/*
!apps/api/prisma/seed-images/README.md

# VS Code (share only team-wide config)
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# OS
Thumbs.db
desktop.ini

# Docker
**/docker-compose.override.yml
docker-compose.override.yml
# Nh蘯｡c n盻］ cho video k盻ｷ ni盻㍊ (45 track .m4a, ~80MB) 窶・tﾃi s蘯｣n nh盻・phﾃ｢n, khﾃｴng vﾃo git.
# L蘯･y v盻・b蘯ｱng cﾃ｡ch copy t盻ｫ demo onemoretime (assets/music) ho蘯ｷc ch蘯｡y l蘯｡i script fetch-music.
apps/api/assets/music/

# Original camera files dropped in for the demo (48 MB of PNG/HEIC/MOV, and
# personal photographs). The app bundles downscaled copies from
# apps/mobile/assets/home instead — 4 JPEGs, ~440 KB, which are committed.
# Regenerate those with the sharp snippet in src/fixtures/home.ts's history.
apps/mobile/assets/Duc/
